projection implementation in class ZBP_RAP_C_EXCEL unique;
strict ( 2 );
extensible;
use draft;
use side effects;

define behavior for ZRAP_C_EXCEL alias EXCEL
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action fileUpload;
  use action fileDownload;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}